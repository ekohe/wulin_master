#content.valign-wrapper
  #screen_content
    - begin
      = render :template => 'homepage/dashboard'
    - rescue

#confirm-modal.info-modal.modal
  .modal-content
    %h5.modal-title 確認
    #confirm-content.modal-message 本当によろしいですか？
  .modal-footer
    #confirmed-btn.btn.right.modal-close はい
    .modal-close.btn-flat.waves-effect いいえ

#error-modal.info-modal.modal
  .modal-content
    %h5.modal-title Error
    #error-content.modal-message Something happened.
  .modal-footer
    .modal-close.btn.right OK

#load-modal.info-modal.modal{style: "width: 150px"}
  .modal-content
    .modal-message Loading...

:javascript
  $(function(){
    // why use setTimeout?
    // Since `.modal` makes the following events not work.
    // Guess Materialize calls the $(selector).modal() automatically in the end
    // We have to make the following events executed after `.modal()`
    setTimeout(() => {
      $('.info-modal').modal({
        onOpenEnd: function(modal, trigger) {
          $(modal).css('z-index', '9999');
        }
      });

      $('#confirm-modal').modal({
        onCloseEnd: function(modal, trigger) {
          $('#confirm-content').text('本当によろしいですか？');
          $('#confirm-modal .modal-title').text('確認');
        }
      });

      $('#error-modal').modal({
        onCloseEnd: function(modal, trigger) {
          $('#error-content').text('エラーが発生しました。');
          $('#error-modal .modal-title').text('エラー');
        }
      });
    })
  });
